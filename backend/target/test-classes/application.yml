spring:
  datasource:
    url: jdbc:h2:mem:mr_system;MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE;DEFAULT_NULL_ORDERING=HIGH
    username: sa
    password:
  flyway:
    enabled: false
  jpa:
    hibernate:
      ddl-auto: create-drop
  mail:
    host: localhost
    port: 1025

app:
  frontend-url: http://localhost:3000
  jwt:
    secret: change-this-development-secret-change-this-development-secret
    access-token-minutes: 30
    refresh-token-days: 14
